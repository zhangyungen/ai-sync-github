## 1. 方案与模型
- [x] 1.1 定义领域模型与枚举（会话、消息、多类型内容、分类标签、同步状态、错误等级），避免魔法值。
- [x] 1.2 设计同步流水线接口（Collector/Normalizer/Classifier/Renderer/Publisher）并确定依赖边界。
- [x] 1.3 明确日志与告警策略：`warn` 用于可降级路径，`error + 告警` 用于不可降级路径。

## 2. 手动同步能力
- [x] 2.1 实现会话列表读取与会话内容预览。
- [x] 2.2 实现手动选择同步会话、目录（分类）和标签确认流程。
- [x] 2.3 实现手动同步发布到 GitHub（首期最小可用路径）。

## 3. 自动同步能力
- [x] 3.1 实现自动同步调度（周期可配置）与启停控制，并校验周期落在可配置范围内。
- [x] 3.2 实现增量游标（lastSyncedMessageId / lastSyncedAt）与前 24 小时窗口去重、幂等检测。
- [x] 3.3 实现失败重试与退避策略，确保自动任务稳定运行。

## 4. 分类与输出
- [x] 4.1 实现“原始 + 分类后”双轨输出目录。
- [x] 4.2 实现角色分类维度：资深市场需求分析师、资深业务架构师、资深产品设计师（需求设计）、资深技术架构师。
- [x] 4.3 实现业务主题与进展情况的动态标签分类维度（非固定枚举），并支持多标签组合。
- [x] 4.4 预留自动分类策略接口；当自动分类不可用或低置信度时回退手动分类。

## 5. GitHub 可视化与安全
- [x] 5.1 实现 Markdown 渲染与索引生成，保证 GitHub 网页可直接浏览。
- [x] 5.2 实现目录时间线组织与会话导航链接。
- [x] 5.3 实现 GitHub App / OAuth / PAT 三种鉴权策略与密钥安全存储（不得明文落库/落仓）。
- [x] 5.4 实现默认直推目标分支发布流程（不强制分支+PR）。

## 6. 测试与验收
- [x] 6.1 为核心路径补充单元测试：标准化、分类、渲染、幂等判断、异常分级。
- [x] 6.2 为同步链路补充集成测试（使用 GitHub API mock）。
- [x] 6.3 完成验收清单：手动同步、自动同步、可视化输出、异常日志与告警均符合 proposal 与 spec。
- [x] 6.4 执行 `openspec validate add-chatgpt-github-sync-extension --strict` 并修复所有问题。
