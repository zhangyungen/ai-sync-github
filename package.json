{
  "name": "chatgpt-sync-github-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for manually and automatically syncing ChatGPT chats to GitHub.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
